# Local files (not committed)
.local/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Ralph per-iteration worktrees (created by ralph.sh / ralph-once.sh)
.ralph-worktrees/
